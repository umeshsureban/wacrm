services:
  wacrm:
    image: ghcr.io/umeshsureban/wacrm:latest
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
